# Contributing

Use Java 21, Docker and `./mvnw spotless:apply verify -Pintegration`. Preserve real Redis concurrency/TTL tests and trusted-principal HTTP tests. Keep Lua bounded and no retries after ambiguous token consumption. Do not commit private credentials or production tenant IDs.
